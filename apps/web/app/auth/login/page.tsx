import { NextPage } from 'next';
import { ReactElement } from 'react';
import { LoginModule } from '@psu-superapp/web-modules';

const LoginPage: NextPage = (): ReactElement => <LoginModule />;

export default LoginPage;
